<div class="comments">
  <h4>Comments</h4>
  {{!-- Iterate over comments --}}
  {{#each comments}}
    <div class="comment">
      <p>{{content}}</p>
      <p>Comment by {{username}} on {{date}}</p>
    </div>
  {{/each}}
</div>